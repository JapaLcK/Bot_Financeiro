{
  "name": "pigbank-frontend-tests",
  "private": true,
  "description": "Só o harness de testes de frontend (tests/frontend/). O site é HTML estático servido pelo backend, com uma ilha React que tem build PRÓPRIO em webapp/; este package.json não tem script build de propósito.",
  "scripts": {
    "test:frontend": "node --test tests/frontend/*.test.mjs",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "globals": "^17.12.0",
    "js-yaml": "^5.4.1",
    "playwright": "^1.56.0"
  }
}
